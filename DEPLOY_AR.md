# دليل تشغيل البوت 24/7 مجانًا بدون إبقاء جهازك مفتوحًا

هذا الدليل يشرح خطوة بخطوة كيف تستضيف البوت مجانًا بالكامل على الإنترنت،
بحيث يستمر بالعمل حتى وأنت نائم أو لابتوبك مغلق.

نستخدم:
1. **GitHub** لرفع كود البوت (مجاني).
2. **Render** لاستضافة وتشغيل البوت (خطة مجانية، بدون بطاقة بنكية).
3. **خدمة "تشعيل دوري" (Uptime Pinger)** مجانية، تمنع Render من إيقاف
   البوت بعد فترة من عدم النشاط.

لن تحتاج لاستخدام أي أوامر طرفية (terminal) -- كل شيء عبر المتصفح.

---

## الجزء الأول: رفع الكود على GitHub

### 1. أنشئ مستودعًا (Repository) جديدًا

1. سجّل الدخول إلى [github.com](https://github.com).
2. اضغط على علامة **+** في الزاوية العلوية اليمنى، ثم **New repository**.
3. اختر اسمًا، مثلًا: `iust-schedule-bot`.
4. اجعله **Private** (خاص) -- هذا مهم لأن المستودع سيحتوي بيانات الجدول.
5. لا تُفعّل "Add a README file" (سنرفع ملفاتنا الجاهزة).
6. اضغط **Create repository**.

### 2. رفع الملفات

1. في صفحة المستودع الجديد (الفارغ)، ستجد رابط **uploading an existing file**.
   اضغط عليه (أو من تبويب "Add file" → "Upload files").
2. اسحب **كل** ملفات ومجلدات البوت دفعة واحدة إلى الصفحة:
   - `bot.py`
   - `schedule_data.py`
   - `pdf_export.py`
   - `arabic_text.py`
   - `extract_schedule.py`
   - `subjects.xlsx`
   - `IUST_schedule_full.xlsx` (إن وُجد لديك)
   - `requirements.txt`
   - `render.yaml`
   - مجلد `fonts/` بالكامل (يحتوي `FreeSerif.ttf` و `FreeSerifBold.ttf`)
3. في أسفل الصفحة، اكتب أي وصف بسيط (مثل "أول رفع للبوت") واضغط
   **Commit changes**.

بعد انتهاء الرفع، تأكد أن كل الملفات ظاهرة في صفحة المستودع، خصوصًا مجلد
`fonts` (افتحه للتأكد من وجود ملفي الخط بداخله).

**مهم جدًا:** لا تكتب توكن البوت (Token) في أي ملف يُرفع على GitHub. سنضع
التوكن في خانة منفصلة وآمنة على Render في الخطوة القادمة، وليس داخل الكود.

---

## الجزء الثاني: استضافة البوت على Render

### 1. أنشئ حسابًا على Render

1. اذهب إلى [render.com](https://render.com) واضغط **Get Started**.
2. سجّل الدخول باستخدام حساب GitHub نفسه (هذا يربطهما تلقائيًا ويسهّل
   الأمر كثيرًا) -- اضغط **Sign up with GitHub** ووافق على الصلاحيات
   المطلوبة.

### 2. أنشئ خدمة جديدة من ملف render.yaml

1. من لوحة تحكم Render، اضغط **New** → **Blueprint**.
2. اختر مستودع `iust-schedule-bot` الذي رفعته (قد تحتاج الضغط على
   **Configure account** لمنح Render صلاحية الوصول لمستودعاتك أول مرة).
3. سيكتشف Render ملف `render.yaml` تلقائيًا ويعرض لك الخدمة المُعرَّفة
   فيه باسم `iust-schedule-bot`.
4. سيطلب منك إدخال قيمتين (لأنهما معرَّفتان كسرّيتين في الملف):
   - **IUST_BOT_TOKEN**: ضع هنا توكن بوتك الذي حصلت عليه من
     [@BotFather](https://t.me/BotFather) في تيليغرام.
   - **WEBHOOK_SECRET**: اكتب أي نص عشوائي طويل تختاره بنفسك (مثلًا
     `myBotSecret2026XYZ`) -- هذا يحمي البوت من استقبال طلبات مزيّفة.
5. اضغط **Apply** أو **Create**.

سيبدأ Render تلقائيًا بتثبيت المتطلبات (`pip install -r requirements.txt`)
وتشغيل البوت (`python3 bot.py`). راقب تبويب **Logs** حتى تظهر رسالة شبيهة
بـ:

```
بدء تشغيل البوت بوضع Webhook على المنفذ 10000 ...
عنوان الـ Webhook: https://iust-schedule-bot-xxxx.onrender.com/webhook
```

هذا يعني أن البوت يعمل بنجاح. إذا فتحت تيليغرام الآن وكتبت `/start` للبوت،
يجب أن يستجيب.

### 3. لاحظ رابط خدمتك

في أعلى صفحة الخدمة على Render، يوجد رابط مثل:

```
https://iust-schedule-bot-xxxx.onrender.com
```

احفظ هذا الرابط، ستحتاجه في الخطوة القادمة.

---

## الجزء الثالث: إبقاء البوت مستيقظًا (خدمة Ping)

خدمة Render المجانية توقف الخدمة تلقائيًا بعد حوالي 15 دقيقة من عدم وصول
أي طلب لها. لمنع هذا، نستخدم خدمة مجانية ترسل طلبًا دوريًا (كل أقل من 15
دقيقة) لرابط `/healthcheck` الذي أضفناه في البوت تحديدًا لهذا الغرض.

### الخيار المُقترح: UptimeRobot

1. اذهب إلى [uptimerobot.com](https://uptimerobot.com) وأنشئ حسابًا
   مجانيًا.
2. اضغط **+ Add New Monitor**.
3. اختر نوع المراقبة: **HTTP(s)**.
4. **Friendly Name**: اكتب أي اسم، مثلًا "IUST Bot".
5. **URL (or IP)**: ضع رابط خدمتك من Render متبوعًا بـ `/healthcheck`،
   مثلًا:
   ```
   https://iust-schedule-bot-xxxx.onrender.com/healthcheck
   ```
6. **Monitoring Interval**: اختر أقل قيمة متوفرة في الخطة المجانية
   (عادة 5 دقائق) -- هذا أقل من حد الـ 15 دقيقة الذي يوقف خدمة Render.
7. اضغط **Create Monitor**.

من الآن فصاعدًا، سترسل UptimeRobot طلبًا كل 5 دقائق لرابط البوت، فلا تحصل
خدمة Render على فرصة لإيقافه أبدًا، وبالتالي يبقى البوت مستجيبًا للجميع
في أي وقت دون أن يحتاج جهازك أن يكون مفتوحًا.

---

## كيف تُحدِّث البوت مستقبلًا

أي تعديل تريده على الكود مستقبلًا (مثلًا تعديل أرسله لك المساعد):

1. اذهب لصفحة المستودع على GitHub.
2. افتح الملف الذي تغيّر (مثلًا `bot.py`).
3. اضغط أيقونة القلم (✏️) للتعديل، أو اضغط **Add file → Upload files**
   لرفع نسخة جديدة فوق القديمة بنفس الاسم.
4. اضغط **Commit changes**.

Render يراقب مستودعك تلقائيًا، وكل مرة تحدّث فيها أي ملف، سيعيد نشر
البوت تلقائيًا بالنسخة الجديدة في خلال دقيقة أو دقيقتين. يمكنك متابعة ذلك
من تبويب **Events** أو **Logs** في لوحة تحكم Render.

---

## ملاحظات مهمة

- **القرص على Render مؤقت**: أي ملف يُنشئه البوت أثناء التشغيل (مثل
  `IUST_schedule_full.xlsx` بعد الضغط على "تحديث أوقات الجدول") يُحذف إذا
  أعاد Render تشغيل الخدمة (مثلًا بعد نشر تحديث جديد). الملف الذي رفعته
  أنت بنفسه على GitHub يبقى دائمًا كنقطة بداية، لكن أي تحديث يجريه طلاب
  آخرون عبر البوت نفسه يضيع عند إعادة التشغيل. إن أردت بيانات دائمة، سيلزم
  حل تخزين خارجي (قاعدة بيانات أو تخزين سحابي) -- أخبرني إن رغبت بهذا
  وسأضيفه.
- **التشغيل المحلي يبقى متاحًا أيضًا**: إن أردت تجربة البوت على جهازك
  كما كان (Polling)، فقط شغّله بدون ضبط أي متغيرات Webhook، وسيعمل تمامًا
  كما كان سابقًا.
- **التوكن سرّي**: لا تشارك توكن البوت مع أي شخص ولا ترفعه مباشرة داخل
  ملفات الكود على GitHub. خانة `IUST_BOT_TOKEN` في Render هي المكان الآمن
  الوحيد لوضعه.

---

## الجزء الرابع: متابعة المستخدمين وإشعارات فورية على تيليغرام (اختياري)

يمكن للبوت متابعة كل مستخدم وإرسال كل شيء إليك (على بوت تيليغرام خاص بك،
منفصل تمامًا عن بوت الجدول) في كل مرة:
- يظهر مستخدم جديد (فيُخصَّص له رقمه الدائم).
- يتفاعل مستخدم مع البوت (اختيار سنة، اختيار مادة، رجوع، حذف).
- ينجز مستخدم أحد الإجراءات الثلاثة: عرض جدول مواده المختارة، إرسال أوقات
  جميع المواد، أو توليد الجدول المثالي.
- ينجح أو يفشل "تحديث أوقات الجدول".

لكل مستخدم لبوت الجدول **رسالة واحدة فعليًا في كل لحظة** في شات الإشعارات
الخاص بك: عند أي نشاط جديد له، تُحذف رسالته القديمة وتُرسَل رسالة جديدة
بمحتواه المحدَّث، فتظهر دائمًا في **آخر** الشات (لا تبقى مخفية في الأعلى
حتى لو تراكمت رسائل مستخدمين آخرين بعدها).

وهذه الرسالة لا تُرسَل مع كل ضغطة: الدفع يمرّ عبر مُرجِئ (debounce) مدّته
أربع ثوانٍ، فمهما ضغط المستخدم بسرعة تصل رسالة واحدة مجمعّة — بينما
البيانات نفسها تُحفَظ في Redis فورًا عند كل حدث دون أي تأجيل.

### ماذا تحتوي رسالة كل مستخدم

- **الرقم الدائم** `#n`: يُخصَّص مرة واحدة عند أول ظهور ولا يتغير أبدًا،
  حتى لو أُعيد تشغيل البوت أو نُشر إصدار جديد.
- **الهوية**: `@username` والاسم الكامل و`telegram_id` وتاريخ الانضمام.
- **الحالة**: 🟢 يستخدم البوت الآن (نشاط خلال آخر 3 دقائق) أو ⚪ غير نشط،
  مع زمن آخر نشاط وكم مضى عليه.
- **العدادات الثلاثة**: جدول مواد مختارة / جدول كل المواد / جدول مثالي،
  بالإضافة إلى عدد المواد التي اختارها وحذفها وإجمالي إجراءاته.
- **ملاحظة** إن كنت قد كتبت واحدة بأمر `/note`.

سجل الأحداث التفصيلي (آخر 50 حدثًا بتواريخها) يُحفَظ في Redis للمراجعة،
لكنه لا يظهر في الرسالة حتى تبقى قصيرة وقابلة للقراءة — تراه عبر `/user`.

### رسالة اللوحة العامة

بالإضافة لرسائل المستخدمين، توجد **رسالة لوحة واحدة** تُعدَّل في مكانها كل
دقيقة (لا تُحذف وتُعاد، لأنها مرجع ثابت يجب أن يبقى في موقعه)، وتعرض:
إجمالي المستخدمين، الجدد اليوم وهذا الأسبوع، من هو متصل الآن، إجمالي كل
إجراء على مستوى البوت، أنشط خمسة مستخدمين، وذروة الاستخدام (الساعة
واليوم). ولا يُرسَل أي طلب إلى تيليغرام إن لم يتغير محتواها فعليًا.

### أوامر مراجعة البيانات (تعمل في شات الإشعارات نفسه)

| الأمر | الوظيفة |
| --- | --- |
| `/help` | قائمة الأوامر |
| `/stats` | الإحصائيات العامة عند الطلب |
| `/users` | آخر 20 مستخدمًا انضمامًا (ويمرّر عدد: `/users 50`) |
| `/user <معرّف أو رقم أو @اسم>` | بطاقة كاملة مع سجل آخر 15 حدثًا |
| `/note <معرّف أو رقم> <نص>` | ملاحظة مراجعة تُحفظ على سجل المستخدم |
| `/export csv` أو `/export json` | تفريغ كل البيانات كملف |
| `/reindex` | إعادة بناء الفهارس من السجلات نفسها |

كل أمر يتحقق أن الشات هو `NOTIFIER_CHAT_ID` نفسه، وأي طلب من أي شات آخر
يُرفَض ويُسرَّد في الـ Logs.

`/note` هي أداة "المراجعة والإكمال لاحقًا": تفتح بطاقة المستخدم بـ `/user`،
تكتب ما لاحظته، فتبقى الملاحظة ظاهرة في رسالته وفي بطاقته حتى تعود إليها.
وملف التصدير CSV يبدأ بـ BOM حتى يفتح في Excel بعربية سليمة.

### 1. أنشئ بوت تيليغرام خاص بالإشعارات

1. افتح [@BotFather](https://t.me/BotFather) واكتب `/newbot`.
2. اختر له اسمًا (مثلًا: `IUST Bot Notifier`) ومعرّفًا ينتهي بـ `bot`.
3. احفظ التوكن الذي يعطيك إياه (مختلف عن توكن بوت الجدول الأساسي).
4. افتح بوتك الجديد واضغط **Start** مرة واحدة (ضروري قبل أن يستطيع
   إرسال أي رسالة إليك).

> **لا تستخدم توكن بوت الجدول نفسه هنا.** أوامر الإدارة تعمل بـ long polling
> على بوت الإشعارات، وتيليغرام لا يسلّم تحديثات بوت واحد إلى مستمعَين —
> فلو تطابق التوكنان سيكتشف النظام ذلك ويرفض تشغيل أوامر الإدارة مسجّلًا
> خطأً واضحًا في الـ Logs (ويبقى بوت الجدول يعمل بشكل طبيعي).

### 2. احصل على معرّفك الشخصي (Chat ID)

1. ابحث عن `@userinfobot` في تيليغرام وابدأ محادثة معه.
2. سيرسل لك رقمًا (Id) -- هذا معرّفك الشخصي.

### 3. أنشئ قاعدة بيانات مجانية على Upstash (للتخزين الدائم)

هذه الخطوة ضرورية لأن قرص Render مؤقت، وبدونها سيُعاد إرسال رسالة كل
مستخدم من الصفر بعد كل إعادة نشر للكود، بدل تحديث رسالته القديمة. والأهم:
بدونها تضيع الأرقام الدائمة والعدادات، لأن الذاكرة وحدها لا تنجو من إعادة
التشغيل.

1. اذهب إلى [upstash.com](https://upstash.com) وأنشئ حسابًا مجانيًا
   (يمكن تسجيل الدخول مباشرة بحساب GitHub).
2. من لوحة التحكم، اضغط **Create Database**.
3. اختر اسمًا (مثلًا `iust-bot-notifier`)، واختر النوع **Regional** (وليس
   Global)، واختر أقرب منطقة جغرافية متاحة.
4. اضغط **Create**.
5. في صفحة القاعدة، انزل لقسم **REST API**، واحفظ القيمتين:
   - `UPSTASH_REDIS_REST_URL`
   - `UPSTASH_REDIS_REST_TOKEN`

### 4. أضف القيم الأربعة على Render

1. اذهب لصفحة خدمتك على Render → تبويب **Environment**.
2. أضف المتغيرات الأربعة التالية بالقيم التي حصلت عليها:
   - `NOTIFIER_BOT_TOKEN` (توكن بوت الإشعارات)
   - `NOTIFIER_CHAT_ID` (معرّفك الشخصي)
   - `UPSTASH_REDIS_REST_URL`
   - `UPSTASH_REDIS_REST_TOKEN`
3. اضغط **Save Changes** -- سيُعيد Render نشر الخدمة تلقائيًا.

لا حاجة لأي متغير جديد لأوامر الإدارة: هي تستخدم `NOTIFIER_BOT_TOKEN`
و`NOTIFIER_CHAT_ID` نفسيهما.

بعد ذلك، أي نشاط في بوت الجدول سيظهر فورًا في محادثتك مع بوت الإشعارات.

### ملاحظات حول هذه الميزة

- إن لم تضبط هذه المتغيرات الأربعة، يستمر بوت الجدول بالعمل بشكل طبيعي
  تمامًا، فقط بدون إرسال أي إشعارات (تُكتب في الـ Logs على Render فقط
  كتحذير، دون أي تأثير على وظائف البوت الأساسية).
- المستوى المجاني لدى Upstash يكفي بسهولة لمئات المستخدمين (256 ميجابايت
  وحتى 500,000 عملية شهريًا)، وهو مجاني دائمًا بلا حاجة لبطاقة بنكية ولا
  تنتهي صلاحيته كقواعد بيانات Render المجانية. وقد خُفّض الاستهلاك أصلًا:
  كل حدث يُحدّث السجل وكل الفهارس والإحصائيات العامة في **طلب واحد** عبر
  نقطة `/pipeline`، وكتابات الحضور مخنوقة زمنيًا (كل 20 ثانية لكل مستخدم).
- **السجلات القديمة لا تضيع**: إن كانت لديك بيانات من الإصدار السابق (حقل
  `schedules_created` وسجل أحداث بدون أرقام دائمة)، تُرحَّل تلقائيًا عند أول
  تفاعل لكل مستخدم: يُخصَّص له رقم دائم، وتُحتسب عدّاداته من سجله القديم،
  ويُبني له موضع في فهرس الانضمام من `join_date` الأصلي.
- على خطة Render المجانية تنام الخدمة عند غياب الطلبات، فتتوقف مهمة تحديث
  اللوحة معها. اللوحة عرض لحالة Redis وليست بيانات، فتستأنف التحديث من
  أول إقلاع دون أن يفقد أي عدّاد قيمته.
- إن لاحظت يومًا أن الإحصائيات لا تطابق الواقع (بعد تفريغ قاعدة مثلًا)،
  نفّذ `/reindex` لإعادة بناء الفهارس من السجلات دون المساس بالأرقام
  الدائمة المخزَّنة داخلها.

### اختبار النظام قبل النشر

الاختبارات تعمل بلا شبكة ولا توكنات، عبر Redis وهمي في الذاكرة:

```bash
python -m unittest test_notifier test_notifier_wiring -v
```

