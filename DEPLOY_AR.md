# دليل تشغيل البوت 24/7 مجانًا بدون إبقاء جهازك مفتوحًا

هذا الدليل يشرح خطوة بخطوة كيف تستضيف البوت مجانًا بالكامل على الإنترنت،
بحيث يستمر بالعمل حتى وأنت نائم أو لابتوبك مغلق.

نستخدم:
1. **GitHub** لرفع كود البوت (مجاني).
2. **Render** لاستضافة وتشغيل البوت (خطة مجانية، بدون بطاقة بنكية).
3. **خدمة "تشعيل دوري" (Uptime Pinger)** مجانية، تمنع Render من إيقاف
   البوت بعد فترة من عدم النشاط.

لن تحتاج لاستخدام أي أوامر طرفية (terminal) -- كل شيء عبر المتصفح.

---

## الجزء الأول: رفع الكود على GitHub

### 1. أنشئ مستودعًا (Repository) جديدًا

1. سجّل الدخول إلى [github.com](https://github.com).
2. اضغط على علامة **+** في الزاوية العلوية اليمنى، ثم **New repository**.
3. اختر اسمًا، مثلًا: `iust-schedule-bot`.
4. اجعله **Private** (خاص) -- هذا مهم لأن المستودع سيحتوي بيانات الجدول.
5. لا تُفعّل "Add a README file" (سنرفع ملفاتنا الجاهزة).
6. اضغط **Create repository**.

### 2. رفع الملفات

1. في صفحة المستودع الجديد (الفارغ)، ستجد رابط **uploading an existing file**.
   اضغط عليه (أو من تبويب "Add file" → "Upload files").
2. اسحب **كل** ملفات ومجلدات البوت دفعة واحدة إلى الصفحة:
   - `bot.py`
   - `schedule_data.py`
   - `pdf_export.py`
   - `arabic_text.py`
   - `extract_schedule.py`
   - `subjects.xlsx`
   - `IUST_schedule_full.xlsx` (إن وُجد لديك)
   - `requirements.txt`
   - `render.yaml`
   - مجلد `fonts/` بالكامل (يحتوي `FreeSerif.ttf` و `FreeSerifBold.ttf`)
3. في أسفل الصفحة، اكتب أي وصف بسيط (مثل "أول رفع للبوت") واضغط
   **Commit changes**.

بعد انتهاء الرفع، تأكد أن كل الملفات ظاهرة في صفحة المستودع، خصوصًا مجلد
`fonts` (افتحه للتأكد من وجود ملفي الخط بداخله).

**مهم جدًا:** لا تكتب توكن البوت (Token) في أي ملف يُرفع على GitHub. سنضع
التوكن في خانة منفصلة وآمنة على Render في الخطوة القادمة، وليس داخل الكود.

---

## الجزء الثاني: استضافة البوت على Render

### 1. أنشئ حسابًا على Render

1. اذهب إلى [render.com](https://render.com) واضغط **Get Started**.
2. سجّل الدخول باستخدام حساب GitHub نفسه (هذا يربطهما تلقائيًا ويسهّل
   الأمر كثيرًا) -- اضغط **Sign up with GitHub** ووافق على الصلاحيات
   المطلوبة.

### 2. أنشئ خدمة جديدة من ملف render.yaml

1. من لوحة تحكم Render، اضغط **New** → **Blueprint**.
2. اختر مستودع `iust-schedule-bot` الذي رفعته (قد تحتاج الضغط على
   **Configure account** لمنح Render صلاحية الوصول لمستودعاتك أول مرة).
3. سيكتشف Render ملف `render.yaml` تلقائيًا ويعرض لك الخدمة المُعرَّفة
   فيه باسم `iust-schedule-bot`.
4. سيطلب منك إدخال قيمتين (لأنهما معرَّفتان كسرّيتين في الملف):
   - **IUST_BOT_TOKEN**: ضع هنا توكن بوتك الذي حصلت عليه من
     [@BotFather](https://t.me/BotFather) في تيليغرام.
   - **WEBHOOK_SECRET**: اكتب أي نص عشوائي طويل تختاره بنفسك (مثلًا
     `myBotSecret2026XYZ`) -- هذا يحمي البوت من استقبال طلبات مزيّفة.
5. اضغط **Apply** أو **Create**.

سيبدأ Render تلقائيًا بتثبيت المتطلبات (`pip install -r requirements.txt`)
وتشغيل البوت (`python3 bot.py`). راقب تبويب **Logs** حتى تظهر رسالة شبيهة
بـ:

```
بدء تشغيل البوت بوضع Webhook على المنفذ 10000 ...
عنوان الـ Webhook: https://iust-schedule-bot-xxxx.onrender.com/webhook
```

هذا يعني أن البوت يعمل بنجاح. إذا فتحت تيليغرام الآن وكتبت `/start` للبوت،
يجب أن يستجيب.

### 3. لاحظ رابط خدمتك

في أعلى صفحة الخدمة على Render، يوجد رابط مثل:

```
https://iust-schedule-bot-xxxx.onrender.com
```

احفظ هذا الرابط، ستحتاجه في الخطوة القادمة.

---

## الجزء الثالث: إبقاء البوت مستيقظًا (خدمة Ping)

خدمة Render المجانية توقف الخدمة تلقائيًا بعد حوالي 15 دقيقة من عدم وصول
أي طلب لها. لمنع هذا، نستخدم خدمة مجانية ترسل طلبًا دوريًا (كل أقل من 15
دقيقة) لرابط `/healthcheck` الذي أضفناه في البوت تحديدًا لهذا الغرض.

### الخيار المُقترح: UptimeRobot

1. اذهب إلى [uptimerobot.com](https://uptimerobot.com) وأنشئ حسابًا
   مجانيًا.
2. اضغط **+ Add New Monitor**.
3. اختر نوع المراقبة: **HTTP(s)**.
4. **Friendly Name**: اكتب أي اسم، مثلًا "IUST Bot".
5. **URL (or IP)**: ضع رابط خدمتك من Render متبوعًا بـ `/healthcheck`،
   مثلًا:
   ```
   https://iust-schedule-bot-xxxx.onrender.com/healthcheck
   ```
6. **Monitoring Interval**: اختر أقل قيمة متوفرة في الخطة المجانية
   (عادة 5 دقائق) -- هذا أقل من حد الـ 15 دقيقة الذي يوقف خدمة Render.
7. اضغط **Create Monitor**.

من الآن فصاعدًا، سترسل UptimeRobot طلبًا كل 5 دقائق لرابط البوت، فلا تحصل
خدمة Render على فرصة لإيقافه أبدًا، وبالتالي يبقى البوت مستجيبًا للجميع
في أي وقت دون أن يحتاج جهازك أن يكون مفتوحًا.

---

## كيف تُحدِّث البوت مستقبلًا

أي تعديل تريده على الكود مستقبلًا (مثلًا تعديل أرسله لك المساعد):

1. اذهب لصفحة المستودع على GitHub.
2. افتح الملف الذي تغيّر (مثلًا `bot.py`).
3. اضغط أيقونة القلم (✏️) للتعديل، أو اضغط **Add file → Upload files**
   لرفع نسخة جديدة فوق القديمة بنفس الاسم.
4. اضغط **Commit changes**.

Render يراقب مستودعك تلقائيًا، وكل مرة تحدّث فيها أي ملف، سيعيد نشر
البوت تلقائيًا بالنسخة الجديدة في خلال دقيقة أو دقيقتين. يمكنك متابعة ذلك
من تبويب **Events** أو **Logs** في لوحة تحكم Render.

---

## ملاحظات مهمة

- **القرص على Render مؤقت**: أي ملف يُنشئه البوت أثناء التشغيل (مثل
  `IUST_schedule_full.xlsx` بعد الضغط على "تحديث أوقات الجدول") يُحذف إذا
  أعاد Render تشغيل الخدمة (مثلًا بعد نشر تحديث جديد). الملف الذي رفعته
  أنت بنفسه على GitHub يبقى دائمًا كنقطة بداية، لكن أي تحديث يجريه طلاب
  آخرون عبر البوت نفسه يضيع عند إعادة التشغيل. إن أردت بيانات دائمة، سيلزم
  حل تخزين خارجي (قاعدة بيانات أو تخزين سحابي) -- أخبرني إن رغبت بهذا
  وسأضيفه.
- **التشغيل المحلي يبقى متاحًا أيضًا**: إن أردت تجربة البوت على جهازك
  كما كان (Polling)، فقط شغّله بدون ضبط أي متغيرات Webhook، وسيعمل تمامًا
  كما كان سابقًا.
- **التوكن سرّي**: لا تشارك توكن البوت مع أي شخص ولا ترفعه مباشرة داخل
  ملفات الكود على GitHub. خانة `IUST_BOT_TOKEN` في Render هي المكان الآمن
  الوحيد لوضعه.
